{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/current-package/not-optimized/dotnet-cache-performance.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}